% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/flat_ridge.R
\name{check_flat_ridge_trigger}
\alias{check_flat_ridge_trigger}
\title{Check Flat-Ridge Trigger}
\usage{
check_flat_ridge_trigger(
  state,
  sigma_min,
  tol_ridge = 0.001,
  rho_tol = 0.1,
  grad_decrease_max = 0.9,
  g_inf_floor = 1e-06
)
}
\arguments{
\item{state}{List returned by \code{\link{init_flat_ridge_state}} and
maintained by \code{\link{update_flat_ridge_state}}.}

\item{sigma_min}{Minimum regularization floor used by the orchestrator.}

\item{tol_ridge}{Upper bound on \code{lambda_min} that counts as
"poorly conditioned" (default \code{1e-3}).}

\item{rho_tol}{Maximum \code{|rho - 1|} that counts as "near-perfect
model" (default \code{0.1}).}

\item{grad_decrease_max}{Ratio of latest to oldest \code{||g||_inf}
above which the gradient is deemed stagnant (default \code{0.9}).}

\item{g_inf_floor}{Absolute lower bound on \code{||g||_inf}; the trigger
will not fire below this value to avoid firing at true local minima
(default \code{1e-6}).}
}
\value{
\code{TRUE} if the trust-region fallback should be activated,
\code{FALSE} otherwise. Returns \code{FALSE} whenever the window is
not yet full or any diagnostic is non-finite.
}
\description{
Evaluates the four-signal rule against the current sliding window.
}
\keyword{internal}
