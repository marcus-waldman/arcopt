% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qn_updates.R
\name{update_lhybrid}
\alias{update_lhybrid}
\title{Limited-Memory Hybrid BFGS/SR1 Update}
\usage{
update_lhybrid(
  history,
  s,
  y,
  m = 10L,
  bfgs_tol = 1e-10,
  sr1_skip_tol = 1e-08,
  damping_threshold = 0.2
)
}
\arguments{
\item{history}{L-Hybrid history list with s, y, gamma. Can be NULL.}

\item{s}{Step vector: s = x_new - x_old.}

\item{y}{Gradient difference: y = g_new - g_old.}

\item{m}{Maximum number of pairs to store (default: 10).}

\item{bfgs_tol}{BFGS curvature tolerance (default: 1e-10).}

\item{sr1_skip_tol}{SR1 skip threshold (default: 1e-8).}

\item{damping_threshold}{Powell damping threshold (default: 0.2).}
}
\value{
A list with components:
\item{history}{Updated history structure with s, y, gamma.}
\item{update_type}{"lbfgs", "lsr1", or "powell".}
\item{theta}{Damping parameter (1.0 if no damping applied).}
}
\description{
Limited-memory version of \code{\link{update_hybrid}}. Routes between L-BFGS,
L-SR1, and Powell-damped L-BFGS based on curvature conditions.
}
\details{
Routing priority:
\enumerate{
\item L-BFGS if y'*s > bfgs_tol (stable, positive definite)
\item L-SR1 if SR1 denominator OK (allows indefiniteness)
\item Powell-damped L-BFGS (guaranteed update)
}

Uses a unified history format (s, y, gamma) compatible with both
lbfgs_compact_form and lbfgs_multiply_b. Does not use rho since we
don't need the two-loop recursion (lbfgs_multiply).

This is Algorithm 4a-lhybrid from design/pseudocode.qmd.
}
\keyword{internal}
