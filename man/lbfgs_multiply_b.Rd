% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qn_updates.R
\name{lbfgs_multiply_b}
\alias{lbfgs_multiply_b}
\title{L-BFGS Hessian-Vector Product (B*v)}
\usage{
lbfgs_multiply_b(history, v, gamma = NULL)
}
\arguments{
\item{history}{L-BFGS history list with components \code{s}, \code{y}, \code{rho}, \code{gamma}.
Can be NULL (returns gamma*v or just v if gamma not available).}

\item{v}{Vector to multiply (length n).}

\item{gamma}{Initial scaling. If NULL, uses history$gamma or 1.0.}
}
\value{
B*v where B is the L-BFGS Hessian approximation.
}
\description{
Computes B\emph{v (Hessian approximation times vector) from L-BFGS history.
Note: This is different from \code{\link{lbfgs_multiply}} which computes
H}v (inverse Hessian times vector).
}
\details{
Uses compact form representation: B = gamma\emph{I + Psi * inv(M) * Psi'
where Psi = Y - gamma}S and M = Psi' * S.

This is needed for Powell damping in L-Hybrid, which requires s^T B s.
}
\keyword{internal}
