% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/healthy_basin.R
\name{check_healthy_basin_trigger}
\alias{check_healthy_basin_trigger}
\title{Check Healthy-Basin Trigger}
\usage{
check_healthy_basin_trigger(
  state,
  rho_polish = 0.9,
  lambda_min_polish = 0.001,
  g_decay_polish = 0.5,
  g_inf_floor_polish = 1e-08
)
}
\arguments{
\item{state}{List returned by \code{\link{init_healthy_basin_state}} and
maintained by \code{\link{update_healthy_basin_state}}.}

\item{rho_polish}{Minimum acceptable \code{rho} throughout the window
(default \code{0.9}).}

\item{lambda_min_polish}{Minimum acceptable \code{lambda_min(H)}
throughout the window (default \code{1e-3}).}

\item{g_decay_polish}{Maximum acceptable ratio of consecutive
\code{||g||_inf} values (default \code{0.5} = 2x-per-iter contraction).}

\item{g_inf_floor_polish}{Absolute lower bound on \code{||g||_inf} at
window start (default \code{1e-8}); prevents firing at convergence.}
}
\value{
\code{TRUE} if the qn_polish transition should be activated,
\code{FALSE} otherwise. Returns \code{FALSE} whenever the window is
not yet full or any diagnostic is non-finite.
}
\description{
Evaluates the five-signal rule against the current sliding window.
}
\keyword{internal}
